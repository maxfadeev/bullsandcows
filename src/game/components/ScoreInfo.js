import React from 'react'

import Message from './Message'

const ScoreInfo = ({score}) => (
  <Message type="success">
    Your score is bottommost-right in the table below
  </Message>
)

export default ScoreInfo